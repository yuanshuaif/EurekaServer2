spring:
  security:
     user:
       name: yuanshuai
       password: dk
  application:
      name: eureka-server
server:
  port: 8082
eureka:
  instance:
      hostname: eureka2
      prefer-ip-address: false
  client:
      serviceUrl:
          registerWithEureka: true
          fetchRegistry: true
          defaultZone: http://yuanshuai:dk@eureka1:8081/eureka/


